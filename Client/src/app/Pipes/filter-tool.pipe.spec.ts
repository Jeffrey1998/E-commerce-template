import { FilterToolPipe } from './filter-tool.pipe';

describe('FilterToolPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterToolPipe();
    expect(pipe).toBeTruthy();
  });
});
